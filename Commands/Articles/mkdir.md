mkdir stands for make directory. this command will make a directory or directories using the following syntax: 

- `[ttilley@somelinux ~]mkdir somedir` to create one directory
- `[ttilley@somelinux ~]mkdir somedir1 somedir2 somedir3` to create multiple directories 
- 