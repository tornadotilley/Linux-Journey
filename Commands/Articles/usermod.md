`usermod` lets us make changes to a user account from the command line. The syntax looks something like this:

```
[ttilley@somelinux ~]$ sudo usermod dani
```

these are some of the options for `usermod`:
![[Pasted image 20260201222306.png]]