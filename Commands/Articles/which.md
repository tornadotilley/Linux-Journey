The `which` command shows us where a command's executable lives on the file system. The syntax looks something like this:

`[ttilley@somelinux ~]$ which ls` 
`/usr/bin/ls`

`which` only works on executable programs, not on builtins nor aliases. 
