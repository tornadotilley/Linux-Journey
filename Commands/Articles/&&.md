Putting `&&` between two commands will make it so that the second command will only execute if the first command is successful. Here is an example:
```
[ttilley@somelinux ~]$ sudo apt update && sudo apt upgrade
```
