`nohup` tells the program to keep running even though the terminal session is closed. This is very handy when we want to launch a program or script, but we want it to run in the background. The syntax looks like this:
```
[ttilley@somelinux ~]$ nohup [command] &
```
